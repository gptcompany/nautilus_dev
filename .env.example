# Redis Cache Configuration (Spec 018)
# Copy to .env and customize for your environment

# Redis Connection
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Optional: Redis SSL (for production)
REDIS_SSL=false

# Optional: Connection timeout (seconds)
REDIS_TIMEOUT=2
