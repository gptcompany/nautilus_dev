# ============================================================================
# Security Environment Variables Template
# ============================================================================
# Copy this file to ~/.secrets/nautilus.env and fill in values.
# Generate secrets with: ./scripts/security/generate_secrets.sh
#
# NEVER commit actual secrets to git!
# ============================================================================

# =============================================================================
# REQUIRED - Trading will fail without these
# =============================================================================

# Hyperliquid trading credentials (CRITICAL)
HYPERLIQUID_PK=                          # 64-char hex private key
HYPERLIQUID_WALLET_ADDRESS=              # 0x... wallet address

# JWT secret for API authentication (min 64 chars)
# Generate: openssl rand -base64 64
JWT_SECRET=

# Neo4j database credentials
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=                          # Generate: openssl rand -base64 32

# Grafana admin credentials
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=                  # Generate: openssl rand -base64 32

# =============================================================================
# OPTIONAL - Enhanced security features
# =============================================================================

# Google Secret Manager (alternative to env vars)
# GCP_PROJECT_ID=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# SECRET_MANAGER_ENABLED=true

# ML Model Security
# Generate keypair: python -m security.ml_security genkey ~/.keys/model.key ~/.keys/model.pub
# MODEL_ENCRYPTION_KEY=                  # 64-char hex (openssl rand -hex 32)
# MODEL_SIGNING_KEY_PATH=~/.keys/model_signing.key
# MODEL_SIGNING_PUBLIC_KEY_PATH=~/.keys/model_signing.pub
# MODEL_AUDIT_LOG_PATH=/var/log/nautilus/model_access.log

# =============================================================================
# RISK CONTROLS - Override defaults if needed
# =============================================================================

# Position limits (% of equity)
# RISK_MAX_POSITION_PCT=10.0             # Default: 10%
# RISK_MAX_SINGLE_POSITION_PCT=5.0       # Default: 5%

# Loss limits (% of equity)
# RISK_MAX_DAILY_LOSS_PCT=3.0            # Default: 3%
# RISK_MAX_WEEKLY_LOSS_PCT=10.0          # Default: 10%

# Leverage and order limits
# RISK_MAX_LEVERAGE=3.0                  # Default: 3x
# RISK_MAX_ORDER_VALUE_USD=100000        # Default: $100k
# RISK_MAX_ORDERS_PER_MINUTE=60          # Default: 60
# RISK_MAX_OPEN_ORDERS_PER_SYMBOL=20     # Default: 20

# Withdrawal controls
# RISK_WITHDRAWAL_WHITELIST=true         # Default: true
# RISK_WITHDRAWAL_DELAY_HOURS=24         # Default: 24h for new addresses
# RISK_MAX_WITHDRAWAL_USD_DAY=50000      # Default: $50k/day

# Trading halt cooldown
# RISK_HALT_COOLDOWN_HOURS=4             # Default: 4h

# Withdrawal whitelist (comma-separated addresses)
# WITHDRAWAL_WHITELIST_ADDRESSES=0x123...,0x456...

# =============================================================================
# NOTIFICATIONS
# =============================================================================

# Discord webhook for alerts
DISCORD_WEBHOOK_URL=

# Telegram bot (optional fallback)
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# Email SMTP (optional fallback)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# ALERT_EMAIL_TO=

# =============================================================================
# MONITORING
# =============================================================================

# QuestDB connection (usually default is fine)
# QUESTDB_HOST=localhost
# QUESTDB_PORT=8812

# Prometheus (usually default is fine)
# PROMETHEUS_URL=http://localhost:9090
