# Hyperliquid Environment Variables
# IMPORTANT: Never commit actual private keys to git!

# =============================================================================
# Testnet Credentials (for development and testing)
# =============================================================================
# Get testnet funds from: https://app.hyperliquid-testnet.xyz/
HYPERLIQUID_TESTNET_PK=0x_your_testnet_private_key_here
HYPERLIQUID_TESTNET_VAULT=  # Optional: vault address for delegated trading

# =============================================================================
# Mainnet Credentials (for production - KEEP SECURE!)
# =============================================================================
# Use a SEPARATE wallet from testnet
# Start with SMALL position sizes
HYPERLIQUID_PK=0x_your_mainnet_private_key_here
HYPERLIQUID_VAULT=  # Optional: vault address for delegated trading

# =============================================================================
# Security Checklist
# =============================================================================
# [ ] Private keys are NOT committed to git
# [ ] Using separate wallets for testnet vs mainnet
# [ ] .env file is in .gitignore
# [ ] Backup of private keys stored securely offline
